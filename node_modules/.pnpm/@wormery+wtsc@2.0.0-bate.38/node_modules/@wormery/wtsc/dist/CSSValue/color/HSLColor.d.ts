import { Percentage } from '../Percentage';
import { Angle } from '../angle/angle';
import { HSLAColor } from './HSLAColor';
import { Color } from './Color';
export interface HSLColor extends Color {
    h: Angle | number;
    s: Percentage;
    l: Percentage;
}
export declare function hsl(h: Angle | number, s: Percentage, l: Percentage): HSLColor;
export declare function hsl(h: Angle | number, s: Percentage, l: Percentage, a: number | Percentage): HSLAColor;
//# sourceMappingURL=HSLColor.d.ts.map